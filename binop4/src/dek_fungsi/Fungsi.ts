namespace fung.dek {

    // export function tampil(f: IFungDek): void {
    //     modul.hal.hapusDaftarSemua();
    //     let namaObj: INama = nama.buat(f.nama, f.id);
    //     namaObj.view.attach(modul.hal.nama());

    //     menu.tambah.fungTbl().style.display = 'block';
    //     menu.tambah.modulTbl().style.display = 'block';

    //     menu.utama.updateTbl().onclick = (e: MouseEvent) => {
    //         e.stopPropagation();
    //     }

    //     menu.ganti(menu.utama.el());
    // }

    // export function updateNama(f: IFungDek): void {
    //     f; //TODO:
    // }
}