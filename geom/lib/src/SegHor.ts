namespace ha.geom {
	/**
	 * segment vs garis x
	 */
	class SegHor {

		checkTabrakanBound(g: IGaris) {
			// b = Garis.boundCollide();
			g; //TODO:
		}

		checkTabrakan(g: IGaris) {
			g; //TODO:
		}

	}
	export const segH: SegHor = new SegHor();

	/**
	 * menghitung garis relatif terhadap titik (0,0)
	 * segement vs titik (0,0)
	 */
	class Seg0 {

	}
	export const seg0: Seg0 = new Seg0();
}